const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/115.0.0.0 Safari/537.36'); await page.goto('https://www.cricbuzz.com/live-cricket-graphs/148415/ind-vs-afg-3rd-odi-afghanistan-tour-of-india-2026', {waitUntil: 'networkidle2'}); const svgs = await page.\$\$eval('svg', svgs => svgs.map(svg => svg.outerHTML)); require('fs').writeFileSync('svgs.json', JSON.stringify(svgs)); console.log('Extracted ' + svgs.length + ' svgs'); await browser.close(); })();
