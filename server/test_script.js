const axios = require('axios'); const cheerio = require('cheerio'); axios.get('https://stats.espncricinfo.com/ci/engine/stats/index.html?class=1;filter=advanced;ground=61;orderby=start;template=results;type=team;view=match', {headers: {'User-Agent': 'Mozilla/5.0'}}).then(res => { const che = cheerio.load(res.data); console.log(che('td:contains(\matches found\)').text().trim()); });
